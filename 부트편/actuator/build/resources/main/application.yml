management:
  endpoints:
    web:
      exposure:
        include: "*"
    jmx:
      exposure:
        include: "health,info"
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true
    java:
      enabled: true
    os:
      enabled: true
info:
  app:
      name: hello-actuator
      company: sk
#  endpoint:
#    shutdown:
#      enabled: true
